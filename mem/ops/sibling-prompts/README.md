# 兄弟アプリ向けプロンプト集の使い方

上流 merge の運用は 2026-08-03 に終了した（`mem/ops/vertical-fork.md`）。
以後、機能はコードではなく**プロンプトで運ぶ**。各ファイルの**水平線（---）から下**を、
そのアプリ（セッコツボード・ストレッチボード・ピラボード・ゴルフボード）の
セッションに丸ごと貼る。

## 書き方の約束（新しいプロンプトを足す人へ）

- **決定事項は固定**し、ファイル名・関数名・SQLSTATE の採番・文言は
  「向こうの現物に合わせて読み替えさせる」書き方にする
- ジムボードで**実際に踏んだ地雷**を必ず載せる（同じ穴に落とさせない）
- 検証の作法（変異検証・本番の3段構え・ROLLBACK）もプロンプト内に含める
- フォーク後のジムボード専用機能に依存する箇所は「あれば繋ぐ」の条件付きにする

## 一覧と貼る順序（2026-08-22 の第1弾）

依存があるものは番号順に。独立しているものはどれからでもよい。

| # | ファイル | 中身 | 依存 |
|---|---|---|---|
| 1 | `windows-scripts-crlf.md` | 🔴 .bat/.ps1 の行が消える問題の点検と修正 | なし。**最優先**（ビルドが静かに壊れる） |
| 2 | `booking-notify-server-side.md` | 予約通知のサーバー側移行（通知が黙って消える不具合の根本対応） | なし |
| 3 | `stripe-comp-guard.md` | Stripe webhook がコンプの店を格下げしないガード | なし |
| 4 | `booking-limits-and-capacity.md` | 予約回数の制限・時間帯別の受け入れ数・免除 | なし |
| 5 | `plan-session-limit.md` | プラン回数上限の強制（allow_overflow） | なし（4 があると免除と並ぶ） |
| 6 | `cycle-pin-and-unit.md` | 起算日の固定＋利用期間の単位＋月末クランプ修正 | **5 を先に**（GB004/GB005/plan_cycle_window を使う） |
| 7 | `blocked-slots-recurrence.md` | くり返しブロック（毎週×N週） | なし |
| 8 | `booking-blocked-windows.md` | 受付しない時間帯（2026-08-21 作成・既存） | なし（4 があれば免除と繋がる） |
| 9 | `workout-memo-visible.md` | 記録のメモを一覧で常時表示（小） | なし |
| 10 | `settings-categories.md` | 店側の設定画面をカテゴリー分けして2階層に | なし（設定画面が長くなったアプリだけ） |

## 2026-09-03 の第2弾（8/23〜9/3 にジムボードへ入った機能）

**どれも上の10本とは独立**なので、好きな順に貼ってよい。下の並びは価値の大きい順。

| # | ファイル | 中身 | 備考 |
|---|---|---|---|
| 11 | `booking-options.md` | 🔴 **予約のオプション（アドオン）**。基本の枠に時間と料金が乗る追加メニュー | 4段階に分けて出せる。**この第2弾で最大かつ最重要**。占有時間の規則を1つ間違えると静かに壊れるので、地雷の節を飛ばさせないこと |
| 12 | `daily-booking-cap.md` | その日の受付を止める（1日の上限人数＋ワンタップ） | `booking-limits-and-capacity.md`（曜日×時間帯）とは別概念。重複しない |
| 13 | `video-library.md` | 自宅向けの動画ライブラリ（YouTube/Vimeo 限定公開URL方式） | 既存の予約・課金に一切触らない自己完結型で**移植の難度が低い**。接骨院の在宅リハビリ・ゴルフの自宅ドリル等、業種ごとに中身を変えるだけ |
| 14 | `data-export-csv.md` | データの CSV 書き出し（顧客・予約ほか） | 🔴 **CSV 機能を入れないアプリも「`profiles.tenant_id` が埋まっているか」の節だけは読む価値がある**（ジムボードは出荷したその日から中身が空だった） |

### まだプロンプトにしていないもの（次に作る候補・価値の高い順）

| 元 PR | 中身 | メモ |
|---|---|---|
| #350 / #352 | 顧客の CSV 一括登録 ＋ 招待メール（アカウントの引き渡し） | 乗り換え時の移行に効く。14 とセット |
| #354 / #356 | 通知の送信履歴を店に見せる ＋ 履歴からの再送 | `booking-notify-server-side.md` の自然な続き |
| #358 | カルテの活動タイムライン（来店・入金・同意ほかを1本に） | ⚠️ 記録・測定はピラボード以外 OFF なので、**他3業種は縮小版**になると伝えること |
| #346 | 店の開設を1トランザクションにする（孤児テナント・マスタ0件を断つ） | 機能というより堅牢化パターン |
| #364後半 ＋ #365 | チャット入力欄がキーボード／下部ナビの裏に隠れる | 🔴 **機能ではなく共有バグ。** 同じ入力欄を引き継いでいるなら同じ穴がある。2段階の顛末（`visualViewport` 対応 → ハードコードした余白が実機の nav 高さと合わずまた隠れた）ごと渡すこと |
| #366 | 予約時間の表示不具合（二重括弧・端数の分数が選べない） | 同じく共有バグ |
| #360 | 「使い切り（3/3）」の表示をエラーから完了に変える | 小。`plan-session-limit.md` への追記で足りる |

### 🔴 作らないもの（方針の壁）

- **体験CRM強化**（#357）と**体験メールのキャンセル案内の店ごと文章化**（#362）は、
  どちらも体験予約が動いていることが前提。**4兄弟とも `TRIAL_BOOKING_ENABLED = false`**
  （「体験予約はジム特有の集客手法。ジムボード以外は全て無くす」2026-08-02 決定。
  `mem/ops/vertical-presets/*.md` に明記）なので、**移植先が存在しない。**
  この方針が変わったときに初めて作る。

### 参考にとどめるもの

- **品質のラチェット**（#359・CI で strict 化と巨大ファイル/`as any` の上限を固定）は
  顧客機能ではなく開発プロセスの決め事。移植プロンプトではなく、やるかどうかの判断から
- **記録入力の種目追加時スクロール**（#341）は `WORKOUT_LOG_ENABLED` が true の
  ピラボードだけが対象で、そのピラボード自身が記録画面を機材向けに作り替え済み。
  該当 UI があれば参考程度に
