/**
 * テナント指定なしの公開ページ（`/trial`・`/drop-in`）で使う既定テナント。
 *
 * **これは互換性のための一時的な措置（レガシーシム）で、マルチテナントSaaSとしては
 * 本来あってはならない挙動**。既定テナント宛に予約が作られるため、別ジムのお客様が
 * うっかりこのURLを踏むと、そのジムではなく Salute御所南 に予約が入ってしまう。
 *
 * それでも残している理由: Salute の LINE・SNS・印刷物などに、テナントIDを含まない
 * `app.kyoto-salute.com/trial` という形のリンクが既に出回っており、これを消すと
 * それらのリンクが一斉に壊れるため。
 *
 * **撤去する手順**:
 *   1. Salute 側の既存リンクを `/trial/:tenantId` 形式（ジム設定の「体験予約リンク」で
 *      コピーできるURL）に貼り替える
 *   2. このファイルを削除し、`TrialBooking.tsx` / `DropInBooking.tsx` の
 *      フォールバックを外して「予約リンクが正しくありません」を表示するようにする
 *
 * 各ジムがジム設定からコピーするリンクは元々 `/trial/:tenantId` 形式なので、
 * 他ジムがこの既定値に依存することはない。
 */
export const LEGACY_DEFAULT_TENANT_ID = "ceda19b0-d5e0-4928-ab2e-996a0b823af4";
